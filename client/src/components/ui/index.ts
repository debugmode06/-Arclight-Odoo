/**
 * Shared UI Components — Component Registry
 * Owner: Member 1
 *
 * This file documents all shared UI components that MUST be built.
 * All module owners should use components from this directory.
 * DO NOT create one-off styled components inside module folders.
 *
 * Status legend:
 * ✅ Built      — Component is ready to use
 * 🔲 Pending   — Component needs to be implemented by Member 1
 *
 * ─── Primitive Components (shadcn/ui wrappers) ──────────────────────────
 *
 * 🔲 Button           — Primary, secondary, ghost, danger variants
 * 🔲 Input            — Text input with label and error state
 * 🔲 Select           — Dropdown select with search
 * 🔲 Dialog           — Modal dialog
 * 🔲 Drawer           — Slide-in side panel
 * 🔲 Card             — Content card with optional header/footer
 * 🔲 Badge            — Status badge
 * 🔲 Tabs             — Tabbed navigation
 * 🔲 Table            — Base table component
 * 🔲 DataTable        — Full-featured sortable/filterable table
 * 🔲 Dropdown         — Dropdown menu
 * 🔲 Tooltip          — Hover tooltip
 * 🔲 Toast            — Toast notification
 * 🔲 Progress         — Progress bar
 *
 * ─── Domain Components ────────────────────────────────────────────────────
 *
 * 🔲 StatusPill       — Colored pill for entity statuses
 * 🔲 MetricCard       — KPI card with value, label, and trend
 * 🔲 LoadingState     — Skeleton/spinner loading placeholder
 * 🔲 EmptyState       — Empty state with icon and message
 * 🔲 ErrorState       — Error state with retry option
 * 🔲 PageHeader       — Page title + breadcrumb + action slot
 * 🔲 ConfirmDialog    — Generic confirm/cancel dialog
 * 🔲 UserAvatar       — Avatar with initials fallback
 *
 * ─── Exports ──────────────────────────────────────────────────────────────
 * (Uncomment as components are implemented)
 */

// Placeholder export to prevent import errors
export {};
